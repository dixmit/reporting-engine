This module is inspired in mis-builder and intends to replace it.
This decision was taken in order avoid collissions with the current state of art of the solution.
Also, we cannot ensure that they will be a smooth transition between both models.

Also, this system is designed to be pluggable easily on any Odoo model by small glue modules.
MIS-Builder was deeply attached to accounting, so it makes sense to reformulate it.
